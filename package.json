{
  "name": "ams-bookings",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server -c-1 .",
    "deploy": "webpack && shx mkdir -p out && shx cp index.html out && shx cp -r dist static out && gh-pages -d out"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.15.3",
    "gh-pages": "^0.12.0",
    "googleapis": "^16.0.0",
    "http-server": "^0.9.0",
    "moment": "^2.17.1",
    "node-sass": "^4.1.1",
    "sass-loader": "^4.1.1",
    "shx": "^0.2.1",
    "vue-resource": "^1.0.3"
  }
}
